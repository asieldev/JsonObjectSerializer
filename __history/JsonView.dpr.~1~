program JsonView;

uses
  System.StartUpCopy,
  FMX.Forms,
  JsonGeneric in 'JsonGeneric.pas' {Form1},
  JsonUtilsView in 'JsonUtilsView.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
